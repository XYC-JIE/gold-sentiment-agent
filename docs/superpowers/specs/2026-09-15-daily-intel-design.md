# 金银舆情 AI 监控系统 — 设计文档

日期：2026-09-15
状态：已确认，待拆实施计划

---

## 一、背景与目标

### 1.1 项目目的

这是一个**求职作品项目**，服务于简历和面试，不是纯粹的自用工具。

判据不是"我每天早上能不能读到新闻"，而是：

1. 面试官点开 GitHub 或看板链接，**3 分钟内能看懂这是什么、做了什么**
2. 你自己能围绕它**讲 20 分钟**，包括设计决策、指标定义、方法局限

用户背景：数据分析方向求职者，技能栈 SQL / Excel / Tableau / Python(Pandas/NumPy/Matplotlib)，已有 FastAPI 后端与微信小程序项目经验。

### 1.2 岗位要求对应关系

| 岗位要求 | 本项目如何命中 |
|---|---|
| 1. 有搭建 AI Agent、自动化工作流（Coze、Dify、n8n 等）实战经验 | 核心交付物是基于 **Dify** 的 LLM 工作流编排（可视化画布 + 导出 DSL 存仓库）；外围是 GitHub Actions 自动化流水线 |
| 2. 能展示用 AI 完成的复杂项目 | LLM 结构化事件抽取 + 指标构建 + 金价对照分析 + 自动可视化分发，构成完整数据分析闭环 |
| 3. 关注 AI 前沿动态，对最新模型发布和技术趋势敏感 | 日报固定包含"AI 前沿"板块；项目本身持续产出可讲述的模型发布时间线 |

### 1.3 非目标

- 不做实时高频快讯（不做自建金十）
- 不做交易信号或投资建议
- 不做 Web 交互式分析平台（静态看板足够）

---

## 二、功能需求

### 2.1 用户可见行为

每天早上约 07:30（北京时间），飞书机器人推送一份日报卡片，包含：

1. **AI 前沿** 3–5 条，每条含标题、一句"为什么重要"、原文链接
2. **金银聚焦** 3–5 条，每条标注影响方向与强度；含当日情绪指数与多空倾向
3. **今日紧盯** 宏观日历（议息、CPI、非农等）
4. **看板链接**

同时仓库内数据与图表持续积累，可供回溯与面试展示。

### 2.2 输入源（中英混合）

**AI 前沿（英文）**：OpenAI News、Anthropic News、Google DeepMind Blog、Hacker News、arXiv cs.AI、Hugging Face Daily Papers

**AI 前沿（中文）**：机器之心、量子位

**金融（中文）**：金十数据快讯、华尔街见闻

**金融（英文）**：CNBC Markets、美联储官方新闻稿

**金价**：stooq.com 日线 CSV（XAUUSD，免费无 key）

> 实施第一步必须逐个验证源的可用性（RSS 地址会变更、部分站点反爬）。所有源地址集中写在 `config/sources.yaml`，失效时替换配置即可，不改代码——这正是配置外置的目的。

---

## 三、整体架构

```
GitHub Actions（cron 每天 23:30 UTC = 次日 07:30 北京时间）
   │
   ① 采集    Python 抓取各源 → 清洗去重 → raw_items
   │
   ② 智能    调用 Dify Workflow API
   │          Dify 内做 LLM 结构化抽取 + 日报文案生成
   │          返回结构化 JSON
   │
   ③ 计算    追加 events → 计算情绪指数 → Matplotlib 出图
   │
   ④ 分发    飞书机器人推送卡片
   │
   ⑤ 沉淀    git commit 数据与图表；生成静态看板并部署 Pages
```

### 3.1 分层职责（核心设计决策）

| 层 | 承担 | 选择理由 |
|---|---|---|
| **Dify** | LLM 编排：结构化抽取、日报生成、Prompt 迭代 | 可视化画布 + 逐节点运行日志可观测；改 Prompt 无需改代码重新部署；LLM 节点可绑定 JSON Schema 约束输出格式 |
| **Python** | 采集、存储、指标计算、出图 | Dify 代码节点是受限沙箱，无 pandas/matplotlib 且不能持久化文件 |
| **GitHub Actions** | 定时调度、结果回写 | 免费额度充足；运行在海外，无需代理即可访问墙外信源；不依赖本机开机 |

**为什么不用 Python 直接调 LLM API、绕过 Dify**：Dify 提供的是可观测、可迭代的编排层。逐节点输入输出与 token 消耗可查，Prompt 迭代不需要改代码和重新部署，结构化输出有 Schema 保证比手写 JSON 解析更稳。

**为什么采集和计算不放进 Dify**：Dify 的代码节点沙箱跑不了 pandas/matplotlib，也不能持久化文件到仓库。这是工具能力边界问题，不是设计取舍。

---

## 四、Dify 工作流设计

五个节点：

```
开始节点（输入 raw_items、date）
  ↓
代码节点：预筛去重 + 每条截断 800 字（控制 token）
  ↓
LLM 节点 A：结构化抽取（JSON Schema 约束）
  ↓
LLM 节点 B：日报文案生成
  ↓
结束节点（返回结构化 JSON）
```

### 4.1 LLM 节点 A 输出结构

每条事件：

| 字段 | 说明 |
|---|---|
| `event_type` | **枚举**：货币政策 / 经济数据 / 地缘政治 / 央行购金 / 美元走势 / 通胀 / 就业 / 其他 |
| `direction` | 利多金银 / 利空金银 / 中性 |
| `strength` | 1–5 整数 |
| `summary` | 中文一句话摘要 |
| `source` / `url` / `published_at` | 溯源信息 |

`event_type` 限定为枚举而非自由文本，否则后续统计时分类会散得无法聚合。

### 4.2 LLM 节点 B 输出结构

- AI 要闻 3–5 条（标题 + "为什么重要"）
- 金银聚焦 3–5 条（含方向与强度）+ 综合判断
- 今日宏观日历

### 4.3 工作流 DSL 导出

Dify 工作流导出为 `dify/daily-intel-workflow.yml` 提交进仓库，作为"确实搭建过 Dify 工作流"的可验证证据，面试时可导入还原。

---

## 五、数据模型与指标定义

两张表，均为每日追加、只增不改。

### 5.1 `data/events.csv`（原始资产）

```
date, event_type, direction, strength, summary, source, url, published_at
```

### 5.2 `data/daily_index.csv`（计算产物）

```
date, sentiment_score, bull_count, bear_count, total_events, gold_close
```

### 5.3 情绪指数定义

每条事件赋予符号：利多 `+1`、利空 `-1`、中性 `0`。

```
sentiment_score = Σ(符号 × strength) / (5 × 事件数)
```

**归一化理由**：除以 `5 × n` 使取值落入 `[-1, 1]`，从而让事件数量不同的日期之间可比。直接求和会导致"新闻多的一天天然分高"，那测的是信息流量而非市场情绪。

### 5.4 金价对照

`gold_close` 抓取 stooq.com 的 XAUUSD 日线。**该列是本项目分析价值的支点**：只有与真实金价并排，才能回答"这个指数是否具备信息量"。缺少它，情绪指数只是一个自娱自乐的分数。

---

## 六、图表与分发产物

三张图，存 `charts/`：

1. 近 30 日情绪指数折线（带 0 轴基准）
2. 近 7 日事件类型分布
3. **情绪指数 vs 金价双轴图** — 面试核心展示物

### 6.1 飞书卡片形态

飞书自定义机器人的富文本要内嵌图片，必须先将图片上传飞书换取 `image_key`，而上传接口需要自建应用凭证，webhook 机器人无法做到。

因此**第一版采用文字卡片 + 看板链接**。真内嵌图片需升级为飞书自建应用，列为可选增强，不影响项目主体价值。

### 6.2 静态看板页

Actions 跑完生成静态 HTML 部署到 GitHub Pages。一箭三雕：面试官点链接即看作品、用户手机随时翻历史、飞书卡片有可引用的链接。

---

## 七、仓库结构

```
gold-sentiment-agent/
├─ .github/workflows/daily.yml      # 定时任务
├─ dify/daily-intel-workflow.yml    # Dify 导出的 DSL
├─ src/
│   ├─ collectors/                  # 采集器，一源一类
│   ├─ dify_client.py               # 调 Dify API
│   ├─ analysis.py                  # 指标计算（纯函数）
│   ├─ charts.py                    # 出图
│   ├─ notifier.py                  # 飞书推送
│   ├─ dashboard.py                 # 生成看板 HTML
│   └─ main.py                      # 串流程，支持 --dry-run
├─ data/                            # events.csv / daily_index.csv
├─ charts/
├─ config/sources.yaml              # 信源配置
├─ docs/                            # 本设计文档、效果截图、方法说明
└─ README.md                        # 面试官第一眼
```

---

## 八、错误处理

**总原则：任何情况下都必须有输出**，最差降级为纯标题列表。一个每日送达的东西，静默失败远比推得难看严重。

| 故障 | 处理策略 |
|---|---|
| 单个源抓取失败 | 各采集器独立 try/except 互不影响，日报中标注"N 个源失败" |
| Dify 调用失败 | 重试 2 次（指数退避）；仍失败则发飞书纯文本告警，绝不静默 |
| LLM 返回非法 JSON | Dify 侧 JSON Schema 约束 + 重试一次；Python 侧二次校验，丢弃坏条目而非整体崩盘 |
| Actions 任务失败 | 依赖 GitHub 自带的失败邮件通知 |

---

## 九、测试策略

| 对象 | 方式 |
|---|---|
| `analysis.py` | 纯函数，给定事件样本断言指数计算结果。**这是 TDD 主要落点** |
| `collectors/` | `requests_mock` 喂固定 RSS/JSON 样本，不依赖真实网络 |
| 端到端 | `--dry-run` 模式：不调 Dify、不推飞书，仅打印结果，本地随时验证 |

---

## 十、配置与成本

### 10.1 密钥清单

| 项 | 存放位置 |
|---|---|
| DeepSeek API Key | **Dify 的模型供应商配置**（不进 Actions） |
| Dify Workflow API Key | GitHub Actions Secrets |
| 飞书 Webhook URL | GitHub Actions Secrets |

### 10.2 成本估算

| 项 | 成本 |
|---|---|
| GitHub Actions | 免费（每天 <1 分钟，远低于 2000 分钟/月额度） |
| Dify Cloud | 免费额度（每天 1 次调用，30 次/月） |
| DeepSeek API | 约几元/月（每天约 20k 输入 + 8k 输出 token） |
| 飞书 | 免费 |

> 具体额度与单价以实施时的官方页面为准。

---

## 十一、简历与面试呈现

### 11.1 简历条目

> **金银舆情 AI 监控系统**｜Dify + GitHub Actions + Python
> 基于 Dify 编排 LLM 工作流，每日自动采集 12 个中英文信源；用 LLM 完成事件结构化抽取（类型／方向／强度），构建日度情绪指数并与金价做趋势比对；飞书机器人自动分发日报，累计运行 N 天、沉淀 M 条结构化事件。
> github.com/XYC-JIE/gold-sentiment-agent

### 11.2 三个必答问题

1. **为什么用 Dify 不直接调 API？** → 编排可观测性、Prompt 迭代无需部署、Schema 约束输出
2. **情绪指数怎么定义的？为什么这么归一化？** → 见 5.3
3. **LLM 抽取准确吗？** → 见 11.3

### 11.3 准确率验证（面试胜负手）

多数 AI 项目止步于"能跑通"，无法回答"有多准"。本项目专门投入一个阶段做这件事：

- 随机抽取 50 条事件**人工标注**作为金标准
- 计算 `event_type` 分类准确率、`direction` 判断准确率
- 输出混淆矩阵图，结果写进 README

这一动作恰好是数据分析师的核心思维：**先定义指标，再验证指标**，是与其他候选人的关键分野。

### 11.4 主动陈述的局限（加分项）

面试时主动说明局限比被追问出来好得多：

- LLM 抽取存在误差，已量化（见 11.3）
- 强度 1–5 由模型主观判断，非拟合得出，故指数是相对指标而非绝对刻度
- 情绪与金价是相关性而非因果，金价同时受实际利率、美元指数、避险需求等多重因素驱动
- 单日事件量小（数条量级），统计功效有限，不能用于高频推断
- 信源可能失效或改版，需持续维护

---

## 十二、实施阶段

| 阶段 | 内容 | 预估 |
|---|---|---|
| 0 | 环境准备：GitHub 仓库、飞书机器人 Webhook、DeepSeek Key、Dify 账号 | 0.5 天 |
| 1 | 采集层：`collectors/` + `sources.yaml` + mock 测试，逐个验证源可用性 | 1 天 |
| 2 | Dify 工作流：搭建、调通、导出 DSL、接通 API | 1 天 |
| 3 | 计算与图表：`analysis.py`（TDD）+ `charts.py` | 0.5 天 |
| 4 | 推送与调度：`notifier.py` + Actions + Secrets 配置 | 0.5 天 |
| 5 | 看板页 + Pages 部署 | 0.5 天 |
| 6 | 准确率验证（50 条人工标注）+ README + 简历文案 | 1 天 |

合计约 5 天工作量（按每天 2–3 小时投入）。

**阶段顺序理由**：1→2 打通"能拿到数据并让 AI 读懂"，3→4 打通"能算出来并送出去"，5→6 才是作品打磨。先让链路端到端跑通一次，再逐段加固。
